import type { ReactNode } from "react";
type IObserverProps = {
    children: () => ReactNode;
    render?: never;
} | {
    children?: never;
    render: () => ReactNode;
} | {
    children?: never;
    render?: never;
};
declare function ObserverComponent({ children, render }: IObserverProps): ReactNode;
declare namespace ObserverComponent {
    var displayName: string;
}
export { ObserverComponent as Observer };
